# Open Germline Database Website

A static website to navigate a germline database.


## Usage

Assuming that the [ogrdb database](https://github.com/matsengrp/ogrdb) is cloned in `../ogrdb`, run
```
$ pip install .
$ python -m ogrdbweb -c "../ogrdb/content"
```
